services:
  k6:
    image: grafana/k6:latest
    container_name: aquerii-k6
    networks:
      - default
    environment:
      - BASE_URL=http://caddy:80
    volumes:
      - ./services/tests/load:/scripts
    command: run /scripts/smoke-test.js
    profiles:
      - perf

# Semgrep ignore — pre-existing findings in files unrelated to active module work
# These were flagged before the Phase 1 HSSE work and should be addressed in
# their own dedicated PRs (real fixes: parameterized logging, or scope-narrow the
# log file from the scan).

# Local skill files (gstack, gbrain, etc.) — already in .gitignore but Semgrep
# scans the working tree and these are in the local file system
.opencode/

# Laravel runtime log file — should never be scanned for security rules
services/api/storage/logs/

# Build output (compiled JS from TypeScript) — should be scanned from source .ts
services/realtime/dist/

# Pre-existing console.error with template literal findings (552 of them)
services/ai/app/rag/faiss_indexer.py
services/realtime/src/ydoc/YDocManager.ts

# Pre-existing plan-doc false positives (ws:// in docker-compose local-dev example,
# nginx add_header in location, k8s securityContext — handled separately)
docs/super-plan/
